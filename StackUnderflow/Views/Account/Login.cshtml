@{
  Layout = "_Layout";
}

@using StackUnderflow.ViewModels
@model RegisterViewModel

<div class="row">
  <div class="col-md-12">
    <div class="jumbotron">
      <p class="lead">Don't have an account? @Html.ActionLink("Register here.", "Register", "Account")</p>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-md-6">
    @using (Html.BeginForm())
    {
      @Html.LabelFor(user => user.UserName, new { @class = "form-label"})
      @Html.TextBoxFor(user => user.UserName, new { @class = "form-control"})

      @Html.LabelFor(user => user.Password, new { @class = "form-label"})
      @Html.PasswordFor(user => user.Password, new { @class = "form-control"})

      @Html.LabelFor(user => user.ConfirmPassword, new { @class = "form-label"})
      @Html.PasswordFor(user => user.ConfirmPassword, new { @class = "form-control"})
      <br>
      <input type="submit" value="Log" class="btn btn-warning"> 
    }
  </div>
    <div class="col-md-6">
    <h3>Join <span style="color: orange;">stack</span><strong>underflow</strong></h3>
    <hr>
    <p>Discuss the latest trends.</p>
    <p></p>
  </div>
</div>